# Managed by nicerobot/tools.repository (distribute-build)
name: actions
on:
  # Manually re-runnable: re-running a PAST run replays its original commit, so
  # a gate that failed months ago stays red even after main was fixed.
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]
concurrency:
  group: actions-${{ github.ref }}
  cancel-in-progress: true
permissions:
  contents: read
jobs:
  actions:
    # A scheduled run re-lints against the floating ci/actions@v2 image with no push;
    # distribute substitutes the per-repo cron above. Skip forks (no-op for push/PR).
    if: github.event_name != 'schedule' || github.event.repository.fork == false
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v5
        with:
          persist-credentials: false
      - uses: nicerobot/tools.build/ci/actions@v2

# repl documentation

Documentation for [`yupsh/repl`](https://github.com/yupsh/repl). Created from [`nicerobot/template.docs`](https://github.com/nicerobot/template.docs).

- [`public/`](public/) — public-facing docs (a Hugo site; GitHub Pages renders this when the repository is public).
- [`private/`](private/) — internal-only docs and specs (never built or published).

The Pages workflow ships disabled at [`.github/workflows/pages.yml.disabled`](.github/workflows/pages.yml.disabled); enable it after the repository is public.
